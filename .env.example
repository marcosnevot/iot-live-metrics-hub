# Database configuration (local dev only)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=admin
DB_NAME=postgres

# Ingest API key (device-side, local only)
INGEST_API_KEY=dev-ingest-api-key-change-me

# MQTT broker (local dev)
MQTT_BROKER_URL=mqtt://127.0.0.1:1883

# JWT configuration (local dev only)
JWT_SECRET=JWTSuperSecret_ForLocalDev_ChangeMe_0123456789
JWT_EXPIRES_IN=1h

# Human users (local dev only)
ADMIN_USERNAME=admin@local.test
ADMIN_PASSWORD=Admin_Local_Dev_123!
ANALYST_USERNAME=analyst@local.test
ANALYST_PASSWORD=Analyst_Local_Dev_123!
